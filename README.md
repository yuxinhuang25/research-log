# Research Log

This repository is organized as a MkDocs site.

The site source lives under `docs/`. Research is grouped first by theme, then by material type inside each theme.

Run locally with:

```powershell
mkdocs serve
```
